<%= form_with(model: appointmentt, local: true) do |form| %>
  <% if appointmentt.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(appointmentt.errors.count, "error") %> prohibited this appointmentt from being saved:</h2>

      <ul>
        <% appointmentt.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :visit %>
    <%= form.datetime_select :visit %>
  </div>

  <div class="field">
    <%= form.label :physician_id %>
    <%= form.collection_select :physician_id,Physician.all,:id,:name %>
  </div>

  <div class="field">
    <%= form.label :patient_id %>
    <%= form.collection_select :patient_id,Patient.all,:id,:name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
